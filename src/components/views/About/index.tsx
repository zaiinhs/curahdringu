import { Layout } from "@/components/Layout";
import { AboutHeroComponent } from "./Fragments/Hero";

export const AboutView = () => {
  return (
    <Layout>
      <AboutHeroComponent />
    </Layout>
  );
};
